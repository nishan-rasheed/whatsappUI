import 'package:flutter/material.dart';

class CameraDetails extends StatelessWidget {
  const CameraDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(child: Text('this is camera')),
    );
  }
}